using UnityEngine;

// Simple script to test compilation
public class BuildTestHelper : MonoBehaviour
{
    // This script exists only to test compilation
    private void Start()
    {
        Debug.Log("Build test helper loaded - compilation working");
    }
}